# TODO: Add your infrastructure and containerization configurations here.
# Define the frontend, backend, and database services.

version: '3.8'

services:
  backend:
    # build: ../backend
    # ports:
    #   - "8000:8000"
    image: placeholder-backend-image
